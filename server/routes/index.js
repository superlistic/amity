const { logRouter } = require('./logRouter');
const { loginRouter } = require('./loginRouter');
const { registerRouter } = require('./registerRouter');
module.exports = { logRouter, loginRouter, registerRouter };
